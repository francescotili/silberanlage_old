export enum CraneStatus {
  Waiting,
  Working,
}

export enum AuftragStatus {
  Queue,
  Loading,
  Moving,
  Working,
  Waiting,
  Unloading,
  Completed,
}

export enum Priority {
  Low,
  Normal,
  High,
}

/* export enum ProcessType {
  Silver,
  CopperSilver
} */
